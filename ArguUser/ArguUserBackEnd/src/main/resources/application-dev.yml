spring:
  datasource:
    url: jdbc:mysql://localhost:3306/argu_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: argu_web
    password: Qwer12

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    root: INFO
    com.argu: DEBUG

# 개발 환경용 Actuator 설정 (상세 정보 공개)
management:
  endpoint:
    health:
      show-details: always  # 개발 환경에서는 상세 정보 항상 표시


